// Berechnung der Wolkenbasis
    $wolkenbasis_meter = berechneWolkenbasis($gerechnete_temperatur, $taupunkt);

    // Ausgabe der Ergebnisse
    echo "Die Wolkenbasis liegt bei etwa " . round($wolkenbasis_meter, 2) . " Metern Höhe.";

    $conn->close();


    $T1 = $gerechnete_temperatur; // Temperatur am Punkt 1 in °C
    $T2 = $taupunkt; // Temperatur am Punkt 2 in °C
    $d = $wolkenbasis_meter;// Abstand zwischen Punkt 1 und Punkt 2 in Metern

    // Berechnung des Temperaturabfallgradienten
    $gradient = ($T2 - $T1) / $d;
    $gradient1000=abs($gradient*1000);

    // Eingabewerte
    $aktuelleTemperatur = $gerechnete_temperatur; // Temperatur in °C bei 57 Meter Höhe
    $temperaturAbnahmePro1000m = $gradient1000; // Temperaturabnahme pro 1000 Meter in °C

    // Berechnung der Temperaturabnahme pro Meter
    $temperaturAbnahmeProMeter = $temperaturAbnahmePro1000m / 1000;

    // Berechnung der Schneefallgrenze
    $schneefallgrenze = $aktuelleTemperatur / $temperaturAbnahmeProMeter;

    // Ausgabe der Ergebnisse
    echo "<br>Aktuelle Temperatur: " . $aktuelleTemperatur . "°C\n";
    echo "<br>Temperaturabnahme pro 1000 Meter: " . $temperaturAbnahmePro1000m . "°C\n";
    echo "<br>Temperaturabnahme pro Meter: " . number_format($temperaturAbnahmeProMeter, 6) . "°C\n";
    echo "<br>Die Schneefallgrenze liegt bei: " . number_format($schneefallgrenze, 2) . " Meter\n";